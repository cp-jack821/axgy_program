<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.AttrKeyMapper">
    <select id="getAttrKeyVoList" resultMap="AttrKeyVoResultMap">
        select t1.*,t2.id as vid, t2.name as vname from attr_key t1
            left join attr_value t2 on t1.id = t2.attr_key_id
                where t1.is_deleted = 0 and t2.is_deleted = 0
    </select>
    <resultMap id="AttrKeyVoResultMap" type="com.atguigu.lease.web.admin.vo.attr.AttrKeyVo" autoMapping="true">
        <id property="id" column="id" />
        <collection property="attrValueList" ofType="AttrValue">
            <id property="id" column="vid" />
            <result property="name" column="vname" />
        </collection>
    </resultMap>
</mapper>
